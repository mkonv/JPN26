import type { Metadata } from "next";
import { AlertTriangle, ArrowRight, CloudSun, ExternalLink, ShieldCheck } from "lucide-react";
import trip from "@/data/trip.json";
import { SiteLink } from "@/app/ui/site-link";
export const metadata: Metadata={title:"Plan B"};
export default function PlanBPage(){return <><header className="page-hero simple-hero planb-hero"><div className="hero-kicker"><ShieldCheck size={15}/> Карман · аварийный индекс</div><h1>Plan B<br/>по конкретным дням.</h1><p>Источник истины — внутри DayView. Здесь только быстрый индекс сценариев, если день пошёл не по плану.</p></header><section className="page-section first-section"><div className="official-links">{trip.days.filter((d)=>"decision" in d||"alternate" in d).map((d)=><SiteLink href={`/day/${d.id}`} key={d.id}><span>{d.dateLabel.slice(0,16)} · {d.title}</span><small>открыть день</small><ArrowRight size={16}/></SiteLink>)}</div></section><section className="page-section dark-section"><div className="section-heading light-heading"><div><span>универсально</span><h2>Якорь → сокращение → возврат</h2></div></div><p className="dark-copy"><AlertTriangle size={15}/> Не ускорять весь день. Защитить фиксированный вход/поезд, убрать первую гибкую остановку и пересчитать ближайший участок.</p></section><section className="page-section"><div className="weather-links"><a href="https://www.jma.go.jp/bosai/forecast/" target="_blank" rel="noreferrer"><CloudSun size={18}/><span>JMA</span><ExternalLink size={15}/></a><a href="https://live.fujigoko.tv/" target="_blank" rel="noreferrer"><CloudSun size={18}/><span>Камеры Fuji</span><ExternalLink size={15}/></a></div></section></>}
